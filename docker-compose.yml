version: '3'

services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    env_file:
      - .env
    hostname: prod-db
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: always

volumes:
  pg_data: